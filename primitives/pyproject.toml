[project]
name = "primitives"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cli_argparser = "primitives.cli_argparser:grouping_parser"
cli_subparsers = "primitives.cli_argparser:subparsers"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["primitives"]
